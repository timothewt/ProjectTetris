Planning :
- Classes diagram
- Model
- Controller
- View

To Do :
> Classes :
- pieces abstract -> position
- one class per piece (L O I J S Z) -> orientation ect
- pieces generator
- grid -> array 10x20 grid[10][20]
- stats ?
- game -> start, pause, ...

> Methods :
- pieces :
    - rotate
    - fall
    - left right
- grid :
    - clear line -> all pieces fall of 1
    - check if a line is full
    - isFree -> is the space free for a piece to fall
- stats :
    - update score

Notes :
grid array 0 if no piece 1 if piece
generate pieces in an arraylist rand.nextInt(6) with a switch
clock, for each iteration the piece goes down
all in private
5 attributes max by class
no more than 100 lines per class
simple methods
10 methods max per class
package controller, package view, package model
array for orientation with modulo iteration